{
  "permissions": {
    "allow": [
      "Bash(python scripts/main.py test_image.png -v)",
      "Bash(pip install *)",
      "Bash(python -c \"from PIL import Image; import os; import glob; files = glob.glob\\('**/*.png', recursive=True\\) + glob.glob\\('**/*.jpg', recursive=True\\) + glob.glob\\('**/*.jpeg', recursive=True\\); [print\\(f'{f}: {Image.open\\(f\\).size}'\\) for f in files[:20] if os.path.exists\\(f\\)]\")"
    ]
  }
}
